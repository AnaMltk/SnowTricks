{% extends 'base.html.twig' %}

{% block title %}Mot de pass oublié
{% endblock %}

{% block body %}

	<div class="container fill">
    {% for message in app.flashes('reinitialisation') %}
		<div class="alert alert-success">
			{{ message }}
		</div>
	{% endfor %}
		<div class="row justify-content-center">
			<div class="col-md-6">
				<h1 class="mb-5 text-center">Reinitialisez votre mot de passe</h1>
				{{ form_start(form)}}
				<div class="form-group">
					{{ form_row(form.email)}}
				</div>
				{{ form_end(form)}}
			</div>
		</div>
	</div>
{% endblock %}
